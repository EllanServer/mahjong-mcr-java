package top.ellan.mahjong.rules.mcr;

/** Why the current player received their last standard tile. */
public enum McrDrawSource {
    DEAL,
    NORMAL,
    KONG_REPLACEMENT,
    FLOWER_REPLACEMENT
}
